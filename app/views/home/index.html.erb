<div id='chart'></div>
<%= render_chart(@chart, 'chart') %>

<p>
  <b>Select test code to view performance:</b>
  	<%= form_tag("sigmaplot", :method => "post") do %>		
		<%= 	select("test_code_id", "id", TestCode.all.collect {|p| [ p.testExpansion, p.id ] }, {:prompt => 'Select Test'}) %>

		<%= submit_tag("View Performance") %>
	<% end %>
<%= google_report_tag(Sigma.avg_sigma_optimal_week_report) %>


<h2>Quality Specifications Management</h2>
<%= link_to "List Quality Specifications", quality_specifications_path%>
</p>

