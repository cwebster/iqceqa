<%= form_for(@iqc) do |f| %>
  <% if @iqc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@iqc.errors.count, "error") %> prohibited this iqc from being saved:</h2>

      <ul>
      <% @iqc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%= simple_form_for @iqc do |f| %>
  <%= f.input :name, :label => 'IQC Material Name'%>
  <%= f.input :manufacturer, :label => 'Manufacturer'%>
  <%= f.input :notes, :label => 'Notes'%>
  <%= f.input :assigned, :label => 'Does this QC come with assigned values'%>
  <%= f.input :code, :label => 'IQC Code'%>
  <%= f.input :lotno, :label => 'Lot Number of material'%>
  <%= f.input :expirydate, :label => 'Expiry date', as: :string%>
  <%= f.input :dateinuse, :label => 'Date in use', as: :string%>
  <%= f.input :datereconstituted, :label => 'Date reconstituted', as: :string %>
  <%= f.input :numberofaliquots, :label => 'Number of aliquots made' %>
  <%= f.input :storagelocation, :label => 'Storage location'%>
  <%= f.input :storagetemp, :label => 'Storage temperature'%>
  
  <%= f.button :submit %>
<% end %>


<% end %>
