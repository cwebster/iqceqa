<%= form_for@analysers, :html => {:class => "form-horizontal well"} do |f| %>
  <% if @analysers.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@analysers.errors.count, "error") %> prohibited this analyser from being saved:</h2>

      <ul>
      <% @analysers.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<legend>Add/Edit Quality Specification</legend>
<fieldset>
	<div class="control-group">
		<%= label_tag(:AnalyserName, "Name", :class =>"control-label") %>
		<div class="controls">
			<%= f.text_field :AnalyserName %>
		</div>
	</div>

	<div class="control-group">
		<%= label_tag(:AnalyserNote, "Name", :class =>"control-label") %>
		<div class="controls">
			<%= f.text_field :AnalyserNote %>
		</div>
	</div>

	<div class="control-group">
		<%= label_tag(:AnalyserLocation, "Name", :class =>"control-label") %>
		<div class="controls">
			<%= f.text_field :AnalyserLocation %>
		</div>
	</div>

		<div class="form-actions">
			<%= f.submit :class => "btn-primary" %>
		</div>
	<fieldset>
<% end %>
